# General 目录

这个目录存放整个项目级别的组件，第三方组件优先使用 CocoaPods 引入，如有特殊考虑不用 CocoaPods 引入的，请放到 Frameworks 目录下。

如果一个组件只在某一个业务模块下使用，应该跟业务相关的代码一起放在 Scene 下。

放在这里的应该是：
* 组件，具有一般性的，不是针对某个特定场景的
* 可能影响整个项目行为的
* 可能存在相互耦合（所以不能成为独立的第三方组件）
